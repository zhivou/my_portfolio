<hr>

<h2>Add New Hard Skill</h2>
<small>These skills will be presented on the main home page as chart. Please note there can be only 5 hard skills(Doughnut chart). And 5 soft skills(Standard chart)</small>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Percent</th>
      <th>Hard</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% MainSkill.all.each do |main_skill| %>
      <tr>
        <td><%= main_skill.name %></td>
        <td><%= main_skill.percent %></td>
        <td><%= main_skill.hard %></td>
        <td><%= link_to 'Edit', edit_main_skill_path(main_skill) %></td>
        <td><%= link_to 'Destroy', main_skill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Main Skill', new_main_skill_path %>
<hr>