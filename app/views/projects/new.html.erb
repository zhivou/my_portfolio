<div class="container">
  <div class="card">
    <div class="card-body">
      <h1>New Project</h1>

      <%= render 'form', project: @project %>

      <%= link_to 'Back', projects_path %>
    </div>
  </div>
</div>

