<p id="notice"><%= notice %></p>

<h1>My Positions</h1>

<table>
  <thead>
    <tr>
      <th>SYMBOL</th>
      <th>COMPANY</th>
      <th>SHARES</th>
      <th>AVERAHE PRICE</th>
    </tr>
  </thead>

  <tbody>
    <% @positions.each do |position| %>
      <tr>
        <td>SYMBOL</td>
        <td><%= position.monthly_income %></td>
        <td><%= position.shares %></td>
        <td><%= position.average_price %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

Create Widget React:
<div id="react-my-positions-new"></div>
<%= react_component("HelloWorld", { greeting: "Hello from react-rails." }) %>

link_to 'New Income', new_income_path
